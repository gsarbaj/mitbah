// @ts-check

/*** @type {import('next').NextConfig}
 */
const nextConfig = {
    i18n: {
        locales: ['lt'],
        defaultLocale: 'lt'
    }
    /* config options here */
}

export default nextConfig
